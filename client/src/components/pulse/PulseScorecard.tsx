import { useMemo, useState } from "react";
import { AlertTriangle, ArrowDown, ArrowUp, CheckCircle2, Minus, Settings2 } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Tabs, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { trpc } from "@/lib/trpc";
import { PulseMetricObservations } from "@/components/pulse/PulseMetricObservations";

function formatValue(value: number | null, displayFormat: string) {
  if (value == null) return "—";
  if (displayFormat === "percentage") return `${value}%`;
  if (displayFormat === "currency") return new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 2 }).format(value);
  if (displayFormat === "duration") return `${value} min`;
  return new Intl.NumberFormat("en-US", { maximumFractionDigits: 2 }).format(value);
}

function MetricValue({ metric, meetingId, onSaved }: { metric: any; meetingId: string; onSaved: () => void }) {
  const [value, setValue] = useState(metric.current.value == null ? "" : String(metric.current.value));
  const [message, setMessage] = useState("");
  const save = trpc.pulse.scorecard.saveCurrentValue.useMutation({
    onSuccess: () => { setMessage(""); onSaved(); },
    onError: (error) => { setValue(metric.current.value == null ? "" : String(metric.current.value)); setMessage(error.message || "SavvyOS could not save that number. Your prior value is still showing."); },
  });
  if (!metric.canEdit) return <span className="text-lg font-semibold tabular-nums sm:text-sm">{formatValue(metric.current.value, metric.displayFormat)}</span>;
  return <div className="text-right"><Input aria-label={`Current ${metric.name}`} className="min-h-11 w-24 text-right text-base font-semibold tabular-nums" type="number" inputMode="decimal" value={value} onChange={(event) => setValue(event.target.value)} onBlur={() => { if (value.trim() === "") return; const next = Number(value); if (!Number.isFinite(next)) return; save.mutate({ meetingId, metricId: metric.metricId, actualValue: next }); }} disabled={save.isPending} />{message && <p role="alert" className="mt-1 max-w-44 text-xs leading-4 text-destructive">{message}</p>}</div>;
}

function Trend({ metric }: { metric: any }) {
  if (!metric.trend) return null;
  const isUp = /up|rising/i.test(metric.trend); const isDown = /down|declining/i.test(metric.trend);
  const Icon = isUp ? ArrowUp : isDown ? ArrowDown : Minus;
  return <span className="inline-flex items-center gap-1 text-xs text-muted-foreground"><Icon className="h-3.5 w-3.5" aria-hidden="true" />{metric.trend}</span>;
}

function MetricDetail({ metric }: { metric: any }) {
  return <Dialog><DialogTrigger asChild><button type="button" className="text-left font-medium underline-offset-4 hover:underline focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring">{metric.name}</button></DialogTrigger><DialogContent className="max-w-lg"><DialogHeader><DialogTitle>{metric.name}</DialogTitle><DialogDescription>{metric.detail.responsibility}</DialogDescription></DialogHeader><div className="space-y-3 text-sm"><p>{metric.detail.definition || "No plain-language definition has been added in SavvyOS yet."}</p><dl className="grid grid-cols-2 gap-3 rounded-lg bg-muted/50 p-3"><div><dt className="text-muted-foreground">Owner</dt><dd className="font-medium">{metric.detail.ownerName}</dd></div><div><dt className="text-muted-foreground">Target</dt><dd className="font-medium">{formatValue(metric.target, metric.displayFormat)}</dd></div><div><dt className="text-muted-foreground">Cadence</dt><dd className="font-medium capitalize">{metric.cadence}</dd></div><div><dt className="text-muted-foreground">Current value</dt><dd className="font-medium">{formatValue(metric.current.value, metric.displayFormat)}</dd></div></dl><div><p className="font-medium">Recent history</p><div className="mt-2 space-y-1">{metric.periods.map((period: any) => <p key={period.periodStart} className="flex justify-between"><span>{period.label}</span><span className="font-medium tabular-nums">{formatValue(period.value, metric.displayFormat)}</span></p>)}</div></div></div></DialogContent></Dialog>;
}

function ScorecardConfiguration({ meetingId, onChanged }: { meetingId: string; onChanged: () => void }) {
  const utils = trpc.useUtils();
  const configuration = trpc.pulse.scorecard.configuration.useQuery({ meetingId });
  const add = trpc.pulse.scorecard.addMetric.useMutation({ onSuccess: () => { utils.pulse.scorecard.configuration.invalidate({ meetingId }); onChanged(); } });
  const remove = trpc.pulse.scorecard.removeMetric.useMutation({ onSuccess: () => { utils.pulse.scorecard.configuration.invalidate({ meetingId }); onChanged(); } });
  return <Dialog><DialogTrigger asChild><Button type="button" variant="outline" size="sm" className="min-h-11"><Settings2 className="mr-2 h-4 w-4" aria-hidden="true" />Scorecard settings</Button></DialogTrigger><DialogContent className="max-h-[85vh] max-w-xl overflow-y-auto"><DialogHeader><DialogTitle>Scorecard settings</DialogTitle><DialogDescription>Pulse only chooses where a SavvyOS metric appears. Names, owners, targets, cadence, and values stay in SavvyOS.</DialogDescription></DialogHeader><div className="space-y-5"><div><p className="font-medium">Shown in this meeting</p><div className="mt-2 space-y-2">{(configuration.data?.mapped ?? []).map((metric: any) => <div key={metric.mappingId} className="flex min-h-11 items-center justify-between gap-3 rounded-lg bg-muted/50 px-3 py-2"><span>{metric.name}{metric.status !== "active" && <span className="ml-2 text-sm text-muted-foreground">({metric.status === "deleted" ? "deleted in SavvyOS" : "inactive in SavvyOS"})</span>}</span><Button type="button" variant="ghost" size="sm" className="min-h-11" onClick={() => remove.mutate({ meetingId, mappingId: metric.mappingId })}>Remove</Button></div>)}{!(configuration.data?.mapped ?? []).length && <p className="text-sm text-muted-foreground">No SavvyOS metrics are selected for this meeting.</p>}</div></div><div><p className="font-medium">Add an active SavvyOS metric</p><div className="mt-2 space-y-2">{(configuration.data?.available ?? []).filter((metric: any) => !(configuration.data?.mapped ?? []).some((mapped: any) => mapped.metricId === metric.id)).map((metric: any) => <div key={metric.id} className="flex min-h-11 items-center justify-between gap-3 rounded-lg border border-border px-3 py-2"><span className="min-w-0"><span className="block truncate">{metric.name}</span><span className="text-sm text-muted-foreground">{metric.ownerName} · {metric.frequency}</span></span><Button type="button" variant="outline" size="sm" className="min-h-11" onClick={() => add.mutate({ meetingId, metricId: metric.id })}>Add</Button></div>)}</div></div></div></DialogContent></Dialog>;
}

export function PulseScorecard({ section, meetingId, canConfigure = false, onChanged }: { section: any; meetingId: string; canConfigure?: boolean; onChanged: () => void }) {
  const items = section.items ?? [];
  const tabs = section.meta?.tabs ?? [];
  const [cadence, setCadence] = useState<string>(tabs[0] ?? "weekly");
  const visible = useMemo(() => items.filter((metric: any) => metric.cadence === cadence), [items, cadence]);
  return <div className="space-y-3"><div className="flex flex-wrap items-center justify-between gap-2"><Tabs value={cadence} onValueChange={setCadence}><TabsList className="h-auto flex-wrap justify-start">{tabs.map((tab: string) => <TabsTrigger key={tab} value={tab} className="min-h-10 capitalize">{tab === "annually" ? "Annual" : tab}</TabsTrigger>)}</TabsList></Tabs>{canConfigure && <ScorecardConfiguration meetingId={meetingId} onChanged={onChanged} />}</div>{canConfigure && (section.meta?.configurationNotes ?? []).map((note: any) => <p key={note.mappingId} className="flex items-start gap-2 rounded-lg bg-muted/60 p-3 text-sm text-muted-foreground"><AlertTriangle className="mt-0.5 h-4 w-4 shrink-0" aria-hidden="true" />{note.note}</p>)}{!items.length && <p className="rounded-lg bg-muted/60 p-3 text-sm text-muted-foreground">No active SavvyOS metrics are selected for this meeting.</p>}{items.length > 0 && !visible.length && <p className="rounded-lg bg-muted/60 p-3 text-sm text-muted-foreground">No metrics use this cadence.</p>}<div className="hidden overflow-x-auto rounded-lg border border-border sm:block"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-muted/50 text-muted-foreground"><tr><th className="px-3 py-3 font-medium">Metric</th><th className="px-3 py-3 font-medium">Owner</th><th className="px-3 py-3 font-medium">Target</th>{visible[0]?.periods.map((period: any) => <th key={period.periodStart} className="px-3 py-3 text-right font-medium">{period.label}</th>)}</tr></thead><tbody>{visible.map((metric: any) => <tr key={metric.metricId} className="border-t border-border"><td className="px-3 py-3"><MetricDetail metric={metric} /><div className="mt-1 flex flex-wrap gap-2"><Trend metric={metric} />{metric.onTarget === false && <span className="inline-flex items-center gap-1 text-xs font-medium"><AlertTriangle className="h-3.5 w-3.5" aria-hidden="true" />Off target</span>}</div></td><td className="px-3 py-3">{metric.owner.name}</td><td className="px-3 py-3">{formatValue(metric.target, metric.displayFormat)}</td>{metric.periods.map((period: any, index: number) => <td key={period.periodStart} className="px-3 py-3 text-right">{index === 0 ? <MetricValue metric={metric} meetingId={meetingId} onSaved={onChanged} /> : <span className="tabular-nums">{formatValue(period.value, metric.displayFormat)}</span>}</td>)}</tr>)}</tbody></table></div><div className="space-y-3 sm:hidden">{visible.map((metric: any) => <article key={metric.metricId} className="rounded-lg border border-border p-4"><div className="flex items-start justify-between gap-3"><div><MetricDetail metric={metric} /><p className="mt-1 text-sm text-muted-foreground">{metric.owner.name} · target {formatValue(metric.target, metric.displayFormat)}</p></div>{metric.onTarget === false && <span className="inline-flex items-center gap-1 text-xs font-medium"><AlertTriangle className="h-4 w-4" aria-hidden="true" />Off target</span>}{metric.onTarget === true && <CheckCircle2 className="h-5 w-5 text-primary" aria-label="On target" />}</div><div className="mt-4 flex items-end justify-between gap-3"><div><p className="text-xs text-muted-foreground">{metric.current.label}</p><MetricValue metric={metric} meetingId={meetingId} onSaved={onChanged} /></div><Trend metric={metric} /></div><div className="mt-4 grid grid-cols-2 gap-2 text-sm">{metric.periods.slice(1).map((period: any) => <p key={period.periodStart} className="rounded bg-muted/50 p-2"><span className="block text-xs text-muted-foreground">{period.label}</span><span className="font-medium tabular-nums">{formatValue(period.value, metric.displayFormat)}</span></p>)}</div></article>)}</div><PulseMetricObservations meetingId={meetingId} onChanged={onChanged} /></div>;
}
